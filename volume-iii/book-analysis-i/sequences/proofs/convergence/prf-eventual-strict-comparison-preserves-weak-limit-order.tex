\newpage
\phantomsection
\label{prf:eventual-strict-comparison-preserves-weak-limit-order}
\LRAProofFor{thm:eventual-strict-comparison-preserves-weak-limit-order}

\begin{remark*}[Return]
\hyperref[thm:eventual-strict-comparison-preserves-weak-limit-order]{Return to Theorem}
\end{remark*}

\begin{theorem*}[Eventual Strict Comparison Preserves Weak Limit Order]
Let $(x_n)$ and $(y_n)$ be real sequences, and let $A,B\in\mathbb{R}$.
Suppose that $x_n\to A$ and $y_n\to B$.
\begin{enumerate}[label=(\alph*)]
  \item If there exists $N\in\mathbb{N}$ such that $x_n<y_n$ for every
  $n\ge N$, then $A\le B$.
  \item If there exists $N\in\mathbb{N}$ such that $x_n>y_n$ for every
  $n\ge N$, then $A\ge B$.
\end{enumerate}

\smallskip
\noindent
\end{theorem*}

\begin{proof}[Professional Standard Proof]
\LRAProofBodyStart
TODO: professional standard proof for eventual-strict-comparison-preserves-weak-limit-order.
\end{proof}

\begin{proof}[Detailed Learning Proof]
\LRAProofBodyStart
TODO: detailed learning proof for eventual-strict-comparison-preserves-weak-limit-order.
\end{proof}

\begin{remark*}[Proof structure]
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item TODO: list mathematical dependencies.
\end{itemize}
\end{dependencies}

\clearpage
