\subsection*{Geometric Intuition}

\begin{exposition}
Riemann and Darboux weigh each slab by ordinary length, \(\Delta x_i\).
Riemann--Stieltjes changes the ruler. A slab is weighed by how much an
integrator \(\alpha\) changes across it:
\[
  \Delta\alpha_i=\alpha(x_i)-\alpha(x_{i-1}).
\]
If \(\alpha(x)=x\), ordinary Riemann integration returns. If \(\alpha\) jumps,
the integral sees a point mass.
\end{exposition}

\begin{remark*}[Interpretation]
Stieltjes does not repair the discontinuity-set problem. It answers a
different complaint: ordinary integration has only one ruler. By replacing
\(\Delta x_i\) with \(\Delta\alpha_i\), the integral can count regions with
nonuniform weight and can encode discrete masses as jumps of \(\alpha\).
\end{remark*}

\subsection*{Definition}

\begin{definitionbox}{Definition (Total Variation)}
\begin{definition}[Total Variation]
\label{def:total-variation}
The total variation of \(\alpha\) on \([a,b]\) is
\[
  V_a^b(\alpha)=\sup_P\sum_i |\alpha(x_i)-\alpha(x_{i-1})|.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
V_a^b(\alpha)=
\sup_{P}\sum_i\left|\alpha(x_i)-\alpha(x_{i-1})\right|,
\]
where \(P\) ranges over partitions of \([a,b]\).
\end{remark*}

\begin{remark*}[Interpretation]
Total variation measures the full amount of motion made by the integrator on
the interval, counting both upward and downward changes.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:integration-partition]{Partition of a Compact Interval}
  \item \hyperref[def:supremum]{Supremum}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Bounded Variation)}
\begin{definition}[Bounded Variation]
\label{def:bounded-variation-integration}
The function \(\alpha\) has \emph{bounded variation} on \([a,b]\) if
\[
  V_a^b(\alpha)<\infty.
\]
Geometrically, this says the ruler has finite total up-and-down travel.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{HasBoundedVariation}(\alpha,[a,b])
\Longleftrightarrow
V_a^b(\alpha)<\infty.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Bounded variation means that the weighted ruler has finite total travel on the
interval.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:total-variation]{Total Variation}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Proposition (Monotone Implies BV)}
\begin{proposition}[Monotone Functions Have Bounded Variation]
\label{prop:monotone-bounded-variation}
If \(\alpha\) is monotone on \([a,b]\), then \(\alpha\) has bounded variation.
\hyperref[prf:monotone-bounded-variation]{\textit{Go to proof.}}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{Monotone}\bigl(\alpha,\mathsf{OrderedSet}([a,b],\leq)\bigr)
\Longrightarrow
\operatorname{HasBoundedVariation}(\alpha,[a,b]).
\]
\end{remark*}

\begin{remark*}[Interpretation]
If \(\alpha\) is increasing, the increments telescope:
\[
  \sum_i\bigl(\alpha(x_i)-\alpha(x_{i-1})\bigr)=\alpha(b)-\alpha(a).
\]
Bounded variation is the signed analogue of that finite ruler budget. It is
what prevents the weighted sums from accumulating uncontrolled oscillatory
ruler error.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:function-monotone]{Monotone Function}
  \item \hyperref[def:bounded-variation-integration]{Bounded Variation}
  \item \hyperref[def:total-variation]{Total Variation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Riemann--Stieltjes Integral)}
\begin{definition}[Riemann--Stieltjes Integral]
\label{def:riemann-stieltjes-integral}
The Riemann--Stieltjes sum is
\[
  \sum_i f(\xi_i)\bigl(\alpha(x_i)-\alpha(x_{i-1})\bigr).
\]
When these sums have a tag-independent limit as \(\|P\|\to0\), the limit is
denoted \(\int_a^b f\,d\alpha\).
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
\int_a^b f\,d\alpha=L
\Longleftrightarrow
\text{all Riemann--Stieltjes sums converge to }L\text{ as }\|P\|\to0
\text{ independently of tags}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The Riemann--Stieltjes integral is the tag-independent limit of sums whose
widths are measured by increments of \(\alpha\) rather than by ordinary length.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:tagged-partition-integration]{Tagged Partition}
  \item \hyperref[def:partition-mesh]{Mesh of a Partition}
  \item \hyperref[def:bounded-variation-integration]{Bounded Variation}
\end{itemize}
\end{dependencies}

\subsection*{Existence and Applicability Criterion}

\begin{theorembox}{Theorem (Continuous Integrand, BV Integrator)}
\begin{theorem}[Continuous Integrand and BV Integrator]
\label{thm:rs-continuous-bv-existence}
If \(f\) is continuous on \([a,b]\) and \(\alpha\) has bounded variation, then
\(\int_a^b f\,d\alpha\) exists.
\hyperref[prf:rs-continuous-bv-existence]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\operatorname{IsContinuous}(f,[a,b])\land
\operatorname{HasBoundedVariation}(\alpha,[a,b])
\Longrightarrow
\operatorname{IsIntegrable}(f,[a,b],\operatorname{RiemannStieltjesCriterion}(\alpha)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Continuity of the integrand controls oscillation, while bounded variation of
the integrator controls the total weighted error.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:continuous-at-point]{Continuity at a Point}
  \item \hyperref[def:bounded-variation-integration]{Bounded Variation}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
\end{itemize}
\end{dependencies}

\subsection*{Algebraic and Order Properties}

\begin{theorembox}{Theorem (Bilinearity)}
\begin{theorem}[Bilinearity of the Riemann--Stieltjes Integral]
\label{thm:rs-bilinearity}
Whenever the displayed integrals exist,
\[
  \int_a^b (\lambda f+\mu g)\,d\alpha
  =
  \lambda\int_a^b f\,d\alpha+\mu\int_a^b g\,d\alpha
\]
an
d
\[
  \int_a^b f\,d(\lambda\alpha+\mu\beta)
  =
  \lambda\int_a^b f\,d\alpha+\mu\int_a^b f\,d\beta .
\]
\hyperref[prf:rs-bilinearity]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\int_a^b (\lambda f+\mu g)\,d\alpha
=
\lambda\int_a^b f\,d\alpha+\mu\int_a^b g\,d\alpha,
\]
and
\[
\int_a^b f\,d(\lambda\alpha+\mu\beta)
=
\lambda\int_a^b f\,d\alpha+\mu\int_a^b f\,d\beta
\]
whenever all displayed integrals exist.
\end{remark*}

\begin{remark*}[Interpretation]
The Riemann--Stieltjes integral is linear in the integrand and linear in the
integrator under the stated existence assumptions.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Interval Additivity)}
\begin{theorem}[Interval Additivity for the Riemann--Stieltjes Integral]
\label{thm:rs-interval-additivity}
If \(c\in[a,b]\) and the relevant Riemann--Stieltjes integrals exist, then
\[
  \int_a^b f\,d\alpha
  =
  \int_a^c f\,d\alpha+\int_c^b f\,d\alpha .
\]
\hyperref[prf:rs-interval-additivity]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
c\in[a,b]\land
\operatorname{IsIntegrable}(f,[a,b],\operatorname{RiemannStieltjesCriterion}(\alpha))
\Longrightarrow
\int_a^b f\,d\alpha
=
\int_a^c f\,d\alpha+\int_c^b f\,d\alpha.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Splitting the interval at an interior point splits the Stieltjes integral into
the sum of the two subinterval contributions.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
  \item \hyperref[def:partition-refinement]{Refinement of a Partition}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Integration by Parts)}
\begin{theorem}[Riemann--Stieltjes Integration by Parts]
\label{thm:rs-integration-by-parts}
If \(f\) and \(\alpha\) are such that one of
\(\int_a^b f\,d\alpha\) and \(\int_a^b \alpha\,df\) exists, then the other
exists under the same Riemann--Stieltjes convention, and
\[
  \int_a^b f\,d\alpha+\int_a^b \alpha\,df
  =
  f(b)\alpha(b)-f(a)\alpha(a).
\]
\hyperref[prf:rs-integration-by-parts]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\int_a^b f\,d\alpha+\int_a^b \alpha\,df
=
f(b)\alpha(b)-f(a)\alpha(a)
\]
whenever one side exists under the stated Riemann--Stieltjes convention.
\end{remark*}

\begin{remark*}[Interpretation]
Integration by parts transfers the Stieltjes differential from one factor to
the other, leaving only the endpoint product terms.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
  \item \hyperref[def:bounded-variation-integration]{Bounded Variation}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Smooth Integrator Reduction)}
\begin{theorem}[Reduction to Riemann Integration for a \(C^1\) Integrator]
\label{thm:rs-c1-reduction}
If \(\alpha\in C^1([a,b])\) and \(f\) is Riemann-integrable on \([a,b]\), then
\[
  \int_a^b f\,d\alpha=\int_a^b f(x)\alpha'(x)\,dx .
\]
\hyperref[prf:rs-c1-reduction]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\alpha\in C^1([a,b])\land\operatorname{IsIntegrable}(f,[a,b],\operatorname{RiemannCriterion})
\Longrightarrow
\int_a^b f\,d\alpha=\int_a^b f(x)\alpha'(x)\,dx.
\]
\end{remark*}

\begin{remark*}[Interpretation]
When the integrator is continuously differentiable, its Stieltjes increments
are represented by the ordinary density \(\alpha'\).
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
  \item \hyperref[def:riemann-integral]{Riemann Integral}
  \item \hyperref[def:derivative-at-a-point]{Derivative at a Point}
\end{itemize}
\end{dependencies}

\subsection*{Worked Positive Example}

\begin{theorembox}{Theorem (Step Integrator)}
\begin{theorem}[Step Integrators Give Finite Sums]
\label{thm:rs-step-integrator-finite-sum}
If \(\alpha\) is constant except for finitely many jumps at points \(c_k\) and
\(f\) is continuous at each \(c_k\), then
\[
  \int_a^b f\,d\alpha=\sum_k f(c_k)\,\Delta\alpha(c_k).
\]
\hyperref[prf:rs-step-integrator-finite-sum]{\textit{Go to proof.}}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\alpha\text{ has finitely many jumps at }c_k
\land
f\text{ is continuous at each }c_k
\Longrightarrow
\int_a^b f\,d\alpha=\sum_k f(c_k)\,\Delta\alpha(c_k).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The Cauchy common-refinement argument runs again. Uniform continuity makes
\(\omega_f(I)\) small on short slabs, and bounded variation controls
\(\sum_i|\Delta\alpha_i|\). The error has the form
\[
  \sum_i \omega_f(I_i)\,|\Delta\alpha_i|,
\]
so small oscillation multiplied by finite total variation is small.
\end{remark*}

\begin{remark*}[Interpretation]
If \(\alpha\) is a step function with jumps at points \(c_k\), then the
Stieltjes integral collapses to a weighted sum of the values \(f(c_k)\) times
the jumps. This is the conceptual doorway to probability: a cum
ulative
distribution function is an integrator, and expectation is an integral against
that integrator.
\end{remark*}

\begin{remark*}[Interpretation]
Suppose \(\alpha\) is constant except for finitely many jumps
\(\Delta\alpha(c_k)=\alpha(c_k+)-\alpha(c_k-)\). Any sufficiently fine
partition has one tiny slab around each \(c_k\) carrying that jump, while all
other slabs have \(\Delta\alpha_i=0\). If \(f\) is continuous at each \(c_k\),
then the tagged height on the \(k\)-th jump slab is forced toward \(f(c_k)\),
so
\[
  \int_a^b f\,d\alpha=\sum_k f(c_k)\,\Delta\alpha(c_k).
\]
This is why a finite weighted sum is not a different species from an integral:
it is a Stieltjes integral against a discrete ruler.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:riemann-stieltjes-integral]{Riemann--Stieltjes Integral}
  \item \hyperref[def:continuous-at-point]{Continuity at a Point}
  \item \hyperref[def:bounded-variation-integration]{Bounded Variation}
\end{itemize}
\end{dependencies}

\subsection*{Worked Failure Mode}

\begin{remark*}[Interpretation]
The new pathology is real. On \([0,1]\), let \(c=1/2\), let
\[
  \alpha(x)=\mathbf 1_{[c,1]}(x),
  \qquad
  f(x)=\mathbf 1_{[c,1]}(x).
\]
The only nonzero \(\Delta\alpha_i\) occurs on the slab containing \(c\). If the
tag for that slab is chosen left of \(c\), its contribution is \(0\); if the
tag is chosen at or right of \(c\), its contribution is \(1\). This discrepancy
does not shrink with the slab width, because the ruler has a whole unit of
mass concentrated at the jump. Tag independence fails, so the
Riemann--Stieltjes integral does not exist in this convention.
\end{remark*}

\subsection*{What the Failure Motivates Next}

\begin{remark*}[Interpretation]
Stieltjes is a sideways generalization, not the Lebesgue fix. Shared
discontinuities of \(f\) and \(\alpha\) can destroy tag independence, and the
standard existence theorem leans on the finite ruler budget
\(V_a^b(\alpha)<\infty\). Brownian paths have infinite variation almost surely,
so stochastic integration cannot be ordinary pathwise Riemann--Stieltjes
integration.
\end{remark*}

\begin{remark*}[Interpretation]
The next stochastic patch is not ``make Stieltjes a little better.'' Brownian
motion forces a different limiting mode: an \(L^2\) or probabilistic limit
rather than a pathwise Riemann--Stieltjes limit. That is the structural reason
Ito integration is not just Stieltjes integration with a rougher integrator.
\end{remark*}
