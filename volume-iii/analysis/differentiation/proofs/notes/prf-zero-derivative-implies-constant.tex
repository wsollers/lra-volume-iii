\newpage
\phantomsection
\label{prf:zero-derivative-implies-constant}

\begin{remark*}[Return]
\hyperref[thm:zero-derivative-implies-constant]{Return to Theorem}
\end{remark*}

\begin{theorem*}[Zero Derivative Implies Constant]
Let $I := [a,b] \subseteq \mathbb{R}$ be a closed interval, and let $f : I \to \mathbb{R}$ be continuous on $I$ and differentiable on $(a,b)$. If $f'(x) = 0$ for all $x \in (a,b)$, then $f$ is constant on $I$.
\end{theorem*}

\begin{proof}
\textbf{Professional Standard Proof.}~\\
Fix any $x \in I$. By the Mean Value Theorem applied to $f$ on $[a,x]$, there exists $c \in (a,x)$ such that $f(x) - f(a) = f'(c)(x-a) = 0 \cdot (x-a) = 0$. Therefore $f(x) = f(a)$ for all $x \in I$.
\end{proof}

\begin{proof}
\textbf{Detailed Learning Proof.}~\\

\textbf{Step 1.} Fix an arbitrary point in $I$. Let $x \in I$ be arbitrary. The goal is to show $f(x) = f(a)$.

\textbf{Step 2.} Apply the Mean Value Theorem on $[a,x]$. Since $f$ is continuous on $[a,b] \supseteq [a,x]$ and differentiable on $(a,b) \supseteq (a,x)$, the MVT applies to $f$ on $[a,x]$: there exists $c \in (a,x)$ such that
\[
f(x) - f(a) = f'(c)(x-a).
\]

\textbf{Step 3.} Evaluate using the hypothesis. Since $c \in (a,x) \subseteq (a,b)$, the hypothesis gives $f'(c) = 0$. Therefore
\[
f(x) - f(a) = 0 \cdot (x - a) = 0,
\]
so $f(x) = f(a)$.

\textbf{Step 4.} Conclude. Since $x \in I$ was arbitrary, $f(x) = f(a)$ for all $x \in I$. Setting $C = f(a)$, the function $f$ is identically $C$ on $I$.
\end{proof}

\begin{remark*}[Proof structure]
A direct application of the MVT. The hypothesis $f' \equiv 0$ annihilates the right-hand side of the MVT identity, collapsing $f(x) - f(a) = 0$ for every $x$. The only non-trivial input is the MVT itself; everything else is arithmetic.
\end{remark*}

\begin{remark*}[Dependencies]~\\
\begin{itemize}
  \item \hyperref[thm:mean-value-theorem]{Theorem: Mean Value Theorem}
\end{itemize}
\end{remark*}
