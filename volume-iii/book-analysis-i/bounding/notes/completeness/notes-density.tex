% =========================================================
% Density
% =========================================================

\subsection*{Density}




\begin{definitionbox}{Definition (Dense Subset)}
\begin{definition}[Dense Subset]
\label{def:dense-subset}
Let $D\subseteq S\subseteq\mathbb{R}$. The set $D$ is \emph{dense in} $S$ if and only if for every $x\in S$ and every $\varepsilon>0$ there exists $d\in D$ such that $|x-d|<\varepsilon$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
\begin{aligned}
&\text{Let }D\subseteq S\subseteq\mathbb{R}.\\
&D\text{ is dense in }S
\Longleftrightarrow
\forall x\in S\;\forall \varepsilon>0\;\exists d\in D\;\bigl(|x-d|<\varepsilon\bigr).
\end{aligned}
\]
\end{remark*}

\begin{remark*}[Definition predicate reading]
\[
\operatorname{DenseSubset}(D,S)\coloneqq
\forall x\in S\;\forall \varepsilon>0\;\exists d\in D\;\bigl(|x-d|<\varepsilon\bigr).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
\begin{aligned}
&\text{Let }D\subseteq S\subseteq\mathbb{R}.\\
&D\text{ is not dense in }S
\Longleftrightarrow
\exists x\in S\;\exists \varepsilon_0>0\;\forall d\in D\;\bigl(|x-d|\ge \varepsilon_0\bigr).
\end{aligned}
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
\neg\operatorname{DenseSubset}(D,S)\Longleftrightarrow
\exists x\in S\;\exists \varepsilon_0>0\;\forall d\in D\;\bigl(|x-d|\ge \varepsilon_0\bigr).
\]
\end{remark*}

\begin{remark*}[Failure modes]
Density fails when some point of $S$ has a positive-radius neighborhood that contains no point of $D$.
\end{remark*}

\begin{remark*}[Failure mode decomposition]
\[
\neg\operatorname{DenseSubset}(D,S)
=
\underbrace{\exists x\in S}_{\text{point not approximated}}
\;\underbrace{\exists \varepsilon_0>0}_{\text{positive exclusion radius}}
\;\underbrace{\forall d\in D\;\bigl(|x-d|\ge \varepsilon_0\bigr)}_{\text{no point of }D\text{ enters the neighborhood}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Density is an approximation property inside $S$. It does not say that every point of $S$ already belongs to $D$; it says that points of $D$ occur arbitrarily close to every point of $S$. Thus $D$ can be a proper subset of $S$ while still leaving no positive-width gap inside $S$.
\end{remark*}

\NoLocalDependencies
\input{volume-iii/book-analysis-i/bounding/notes/completeness/figure-dense-subset}
\begin{definitionbox}{Definition (Order Dense Subset)}
\begin{definition}[Order Dense Subset]
\label{def:order-dense-subset}
Let $X$ be a linearly ordered set and let $A\subseteq X$. The subset $A$ is \emph{order dense in} $X$ if and only if for every $a,b\in X$ with $a<b$ there exists $c\in A$ such that $a<c<b$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
\begin{aligned}
&\text{Let }X\text{ be a linearly ordered set and }A\subseteq X.\\
&A\text{ is order dense in }X
\Longleftrightarrow
\forall a,b\in X\;\bigl(a<b\rightarrow \exists c\in A\;(a<c<b)\bigr).
\end{aligned}
\]
\end{remark*}

\begin{remark*}[Definition predicate reading]
\[
\operatorname{OrderDenseSubset}(A,X)\coloneqq
\forall a,b\in X\;\bigl(a<b\rightarrow \exists c\in A\;(a<c<b)\bigr).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
\begin{aligned}
&\text{Let }X\text{ be a linearly ordered set and }A\subseteq X.\\
&A\text{ is not order dense in }X
\Longleftrightarrow
\exists a,b\in X\;\bigl(a<b\land \forall c\in A\;(c\le a\lor b\le c)\bigr).
\end{aligned}
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
\neg\operatorname{OrderDenseSubset}(A,X)\Longleftrightarrow
\exists a,b\in X\;\bigl(a<b\land \forall c\in A\;(c\le a\lor b\le c)\bigr).
\]
\end{remark*}

\begin{remark*}[Failure modes]
Order density fails when there is a nonempty open interval of $X$ that contains no point of $A$.
\end{remark*}

\begin{remark*}[Failure mode decomposition]
\[
\neg\operatorname{OrderDenseSubset}(A,X)
=
\underbrace{\exists a,b\in X\;(a<b)}_{\text{nonempty interval}}
\;\land\;
\underbrace{\forall c\in A\;(c\le a\lor b\le c)}_{\text{no point of }A\text{ lies between }a\text{ and }b}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Order density is an interval-penetration property. It ignores metric distance and asks only whether every ordered gap contains a point of the subset. A subset can therefore be order dense in $X$ precisely when no nonempty open interval of $X$ is missed entirely by that subset.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:total-order]{Total Order}
  \item \hyperref[def:subset]{Subset}
\end{itemize}
\end{dependencies}
\begin{theorembox}{Theorem (Density of the Rationals)}
\begin{theorem}[Density of the Rationals]
\label{thm:density-of-rationals-in-reals}
For every $a,b\in\mathbb{R}$ with $a<b$, there exists $q\in\mathbb{Q}$ such that $a<q<b$.
\hyperref[prf:density-of-rationals-in-reals]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\forall a,b\in\mathbb{R}\;\bigl(a<b\rightarrow \exists q\in\mathbb{Q}\;(a<q<b)\bigr).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
\operatorname{OrderDenseSubset}(\mathbb{Q},\mathbb{R})\Longleftrightarrow
\forall a,b\in\mathbb{R}\;\bigl(a<b\rightarrow \exists q\in\mathbb{Q}\;(a<q<b)\bigr).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
\exists a,b\in\mathbb{R}\;\bigl(a<b\land \forall q\in\mathbb{Q}\;(q\le a\lor b\le q)\bigr).
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
\neg\operatorname{OrderDenseSubset}(\mathbb{Q},\mathbb{R})\Longleftrightarrow
\exists a,b\in\mathbb{R}\;\bigl(a<b\land \forall q\in\mathbb{Q}\;(q\le a\lor b\le q)\bigr).
\]
\end{remark*}

\begin{remark*}[Failure modes]
The theorem would fail precisely if some nonempty real interval contained no rational number.
\end{remark*}

\begin{remark*}[Failure mode decomposition]
\[
\neg\operatorname{OrderDenseSubset}(\mathbb{Q},\mathbb{R})
=
\underbrace{\exists a,b\in\mathbb{R}\;(a<b)}_{\text{nonempty real interval}}
\;\land\;
\underbrace{\forall q\in\mathbb{Q}\;(q\le a\lor b\le q)}_{\text{no rational lies inside}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Rational numbers penetrate every real interval. The theorem does not say that every real number is rational; it says that rational numbers are never separated from a real interval by a positive order gap. This is the interval form of rational density in the real line.
\end{remark*}

\begin{dependencies}
\hyperref[def:order-dense-subset]{Definition~\ref*{def:order-dense-subset}},
\hyperref[thm:archimedean-property]{Theorem~\ref*{thm:archimedean-property}}.
\end{dependencies}

\begin{lemmabox}{Lemma (Rational Translation Preserves Rationality)}
\begin{lemma}[Rational Translation Preserves Rationality]
\label{lem:rational-translation-preserves-rationality}
If $r,q\in\mathbb{Q}$, then
\[
r+q\in\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall r,q\in\mathbb{Q}\;(r+q\in\mathbb{Q}).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The rational numbers are closed under addition, so translating a rational by a
rational keeps it rational.
\end{remark*}

\NoLocalDependencies

\begin{lemmabox}{Lemma (Rational Difference Preserves Rationality)}
\begin{lemma}[Rational Difference Preserves Rationality]
\label{lem:rational-difference-preserves-rationality}
If $r,q\in\mathbb{Q}$, then
\[
r-q\in\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall r,q\in\mathbb{Q}\;(r-q\in\mathbb{Q}).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Subtraction is addition of the additive inverse, and rational numbers are
closed under both operations.
\end{remark*}

\NoLocalDependencies

\begin{lemmabox}{Lemma (Nonzero Rational Product Preserves Irrationality)}
\begin{lemma}[Nonzero Rational Product Preserves Irrationality]
\label{lem:nonzero-rational-product-preserves-irrationality}
If $q\in\mathbb{Q}\setminus\{0\}$ and
$x\in\mathbb{R}\setminus\mathbb{Q}$, then
\[
qx\in\mathbb{R}\setminus\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall q\in\mathbb{Q}\setminus\{0\}\;\forall x\in\mathbb{R}\setminus\mathbb{Q}\;
\bigl(qx\in\mathbb{R}\setminus\mathbb{Q}\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Multiplying an irrational by a nonzero rational cannot make it rational,
because division by the same nonzero rational would recover the original
irrational as a rational number.
\end{remark*}

\NoLocalDependencies

\begin{lemmabox}{Lemma (Rational Translation Preserves Irrationality)}
\begin{lemma}[Rational Translation Preserves Irrationality]
\label{lem:rational-translation-preserves-irrationality}
If $q\in\mathbb{Q}$ and $x\in\mathbb{R}\setminus\mathbb{Q}$, then
\[
q+x\in\mathbb{R}\setminus\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall q\in\mathbb{Q}\;\forall x\in\mathbb{R}\setminus\mathbb{Q}\;
\bigl(q+x\in\mathbb{R}\setminus\mathbb{Q}\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
A rational translate of an irrational remains irrational; otherwise
subtracting the rational translation would make the original number rational.
\end{remark*}

\begin{dependencies}
\hyperref[lem:rational-difference-preserves-rationality]{Rational Difference Preserves Rationality}.
\end{dependencies}

\begin{lemmabox}{Lemma (Irrational Minus Irrational Need Not Be Irrational)}
\begin{lemma}[Irrational Minus Irrational Need Not Be Irrational]
\label{lem:irrational-minus-irrational-need-not-be-irrational}
There exist irrational numbers $x,y\in\mathbb{R}\setminus\mathbb{Q}$ such
that
\[
x-y\in\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\exists x,y\in\mathbb{R}\setminus\mathbb{Q}\;(x-y\in\mathbb{Q}).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Taking $x=y=\sqrt{2}$ gives $x-y=0$, so irrationality is not closed under
difference.
\end{remark*}

\NoLocalDependencies

\begin{lemmabox}{Lemma (Rational Minus Irrational Is Irrational)}
\begin{lemma}[Rational Minus Irrational Is Irrational]
\label{lem:rational-minus-irrational-is-irrational}
If $q\in\mathbb{Q}$ and $x\in\mathbb{R}\setminus\mathbb{Q}$, then
\[
q-x\in\mathbb{R}\setminus\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall q\in\mathbb{Q}\;\forall x\in\mathbb{R}\setminus\mathbb{Q}\;
\bigl(q-x\in\mathbb{R}\setminus\mathbb{Q}\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
If $q-x$ were rational, then $x=q-(q-x)$ would be rational, contradicting the
hypothesis on $x$.
\end{remark*}

\begin{dependencies}
\hyperref[lem:rational-difference-preserves-rationality]{Rational Difference Preserves Rationality}.
\end{dependencies}

\begin{lemmabox}{Lemma (Irrational Plus Rational Is Irrational)}
\begin{lemma}[Irrational Plus Rational Is Irrational]
\label{lem:irrational-plus-rational-is-irrational}
If $x\in\mathbb{R}\setminus\mathbb{Q}$ and $q\in\mathbb{Q}$, then
\[
x+q\in\mathbb{R}\setminus\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall x\in\mathbb{R}\setminus\mathbb{Q}\;\forall q\in\mathbb{Q}\;
\bigl(x+q\in\mathbb{R}\setminus\mathbb{Q}\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
This is the commuted form of rational translation preserving irrationality.
\end{remark*}

\begin{dependencies}
\hyperref[lem:rational-translation-preserves-irrationality]{Rational Translation Preserves Irrationality}.
\end{dependencies}

\begin{lemmabox}{Lemma (Irrational Minus Rational Is Irrational)}
\begin{lemma}[Irrational Minus Rational Is Irrational]
\label{lem:irrational-minus-rational-is-irrational}
If $x\in\mathbb{R}\setminus\mathbb{Q}$ and $q\in\mathbb{Q}$, then
\[
x-q\in\mathbb{R}\setminus\mathbb{Q}.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall x\in\mathbb{R}\setminus\mathbb{Q}\;\forall q\in\mathbb{Q}\;
\bigl(x-q\in\mathbb{R}\setminus\mathbb{Q}\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Subtracting a rational is the same as adding its rational additive inverse, so
an irrational remains irrational.
\end{remark*}

\begin{dependencies}
\hyperref[lem:rational-translation-preserves-irrationality]{Rational Translation Preserves Irrationality}.
\end{dependencies}

\input{volume-iii/book-analysis-i/bounding/notes/completeness/figure-rational-irrational-translation}








\begin{theorembox}{Theorem (Density of the Irrationals)}
\begin{theorem}[Density of the Irrationals]
\label{thm:density-of-irrationals-in-reals}
For every $a,b\in\mathbb{R}$ with $a<b$, there exists $s\in\mathbb{R}\setminus\mathbb{Q}$ such that $a<s<b$.
\hyperref[prf:density-of-irrationals-in-reals]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\forall a,b\in\mathbb{R}\;\bigl(a<b\rightarrow \exists s\in\mathbb{R}\setminus\mathbb{Q}\;(a<s<b)\bigr).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
\operatorname{OrderDenseSubset}(\mathbb{R}\setminus\mathbb{Q},\mathbb{R})\Longleftrightarrow
\forall a,b\in\mathbb{R}\;\bigl(a<b\rightarrow \exists s\in\mathbb{R}\setminus\mathbb{Q}\;(a<s<b)\bigr).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
\exists a,b\in\mathbb{R}\;\bigl(a<b\land \forall s\in\mathbb{R}\setminus\mathbb{Q}\;(s\le a\lor b\le s)\bigr).
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
\neg\operatorname{OrderDenseSubset}(\mathbb{R}\setminus\mathbb{Q},\mathbb{R})\Longleftrightarrow
\exists a,b\in\mathbb{R}\;\bigl(a<b\land \forall s\in\mathbb{R}\setminus\mathbb{Q}\;(s\le a\lor b\le s)\bigr).
\]
\end{remark*}

\begin{remark*}[Failure modes]
The theorem would fail precisely if some nonempty real interval contained no irrational number.
\end{remark*}

\begin{remark*}[Failure mode decomposition]
\[
\neg\operatorname{OrderDenseSubset}(\mathbb{R}\setminus\mathbb{Q},\mathbb{R})
=
\underbrace{\exists a,b\in\mathbb{R}\;(a<b)}_{\text{nonempty real interval}}
\;\land\;
\underbrace{\forall s\in\mathbb{R}\setminus\mathbb{Q}\;(s\le a\lor b\le s)}_{\text{no irrational lies inside}}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Irrational numbers also penetrate every real interval. One way to see the result is to insert a rational point into a translated and rescaled interval, then shift it by an irrational amount. The outcome is that the irrational numbers, like the rational numbers, leave no interval gap in $\mathbb{R}$.
\end{remark*}

\begin{dependencies}
\hyperref[def:order-dense-subset]{Definition~\ref*{def:order-dense-subset}},
\hyperref[thm:density-of-rationals-in-reals]{Theorem~\ref*{thm:density-of-rationals-in-reals}}.
\end{dependencies}





\begin{theorembox}{Theorem (An Irrational Between Any Two Rationals)}
\begin{theorem}[An Irrational Between Any Two Rationals]
\label{thm:irrational-between-any-two-rationals}
If $r,s\in\mathbb{Q}$ and $r<s$, then there exists
$x\in\mathbb{R}\setminus\mathbb{Q}$ such that
\[
r<x<s.
\]
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\forall r,s\in\mathbb{Q}\;
\bigl(r<s\rightarrow
\exists x\in\mathbb{R}\setminus\mathbb{Q}\;(r<x<s)\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
This is the density of the irrationals restricted to intervals whose endpoints
are rational.
\end{remark*}

\begin{dependencies}
\hyperref[thm:density-of-irrationals-in-reals]{Density of the Irrationals}.
\end{dependencies}

\begin{theorembox}{Theorem (A Rational Between Any Two Irrationals)}
\begin{theorem}[A Rational Between Any Two Irrationals]
\label{thm:rational-between-any-two-irrationals}
If $x,y\in\mathbb{R}\setminus\mathbb{Q}$ and $x<y$, then there exists
$q\in\mathbb{Q}$ such that
\[
x<q<y.
\]
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
\forall x,y\in\mathbb{R}\setminus\mathbb{Q}\;
\bigl(x<y\rightarrow \exists q\in\mathbb{Q}\;(x<q<y)\bigr).
\]
\end{remark*}

\begin{remark*}[Interpretation]
This is the density of the rationals restricted to intervals whose endpoints
are irrational.
\end{remark*}

\begin{dependencies}
\hyperref[thm:density-of-rationals-in-reals]{Density of the Rationals}.
\end{dependencies}

\begin{lemmabox}{Lemma (Small Irrational Positive Number)}
\begin{lemma}[Small Irrational Positive Number]
\label{lem:small-irrational-positive-number}
For every $\varepsilon>0$, there exists
$\eta\in\mathbb{R}\setminus\mathbb{Q}$ such that
\[
0<\eta<\varepsilon.
\]
\end{lemma}
\end{lemmabox}

\begin{remark*}[Standard quantified statement]
\[
\forall \varepsilon>0\;\exists \eta\in\mathbb{R}\setminus\mathbb{Q}\;
(0<\eta<\varepsilon).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The density of the irrationals applied to the interval $(0,\varepsilon)$ gives
an irrational positive number as small as desired.
\end{remark*}

\begin{dependencies}
\hyperref[thm:density-of-irrationals-in-reals]{Density of the Irrationals}.
\end{dependencies}

\begin{corollarybox}{Corollary (No Adjacent Real Numbers)}
\begin{corollary}[No Adjacent Real Numbers]
\label{cor:no-adjacent-real-numbers}
If $a,b\in\mathbb{R}$ and $a<b$, then there exists $c\in\mathbb{R}$ such that
\[
a<c<b.
\]
\hyperref[prf:no-adjacent-real-numbers]{Go to proof.}
\end{corollary}
\end{corollarybox}
\begin{remark*}[Standard quantified statement]
\[
\forall a,b\in\mathbb{R}\;\bigl(a<b\Rightarrow
\exists c\in\mathbb{R}\;(a<c<b)\bigr).
\]
\end{remark*}
\begin{remark*}[Predicate reading]
\[
\operatorname{OrderDense}(\mathbb{R}).
\]
\end{remark*}
\begin{remark*}[Negated quantified statement]
\[
\exists a,b\in\mathbb{R}\;
\bigl(a<b\land \forall c\in\mathbb{R}\;(c\le a\lor b\le c)\bigr).
\]
\end{remark*}
\begin{remark*}[Negation predicate reading]
\[
\neg\operatorname{OrderDense}(\mathbb{R}).
\]
\end{remark*}
\begin{remark*}[Interpretation]
No two distinct real numbers are consecutive; every nonempty open real interval
contains another real point.
\end{remark*}
\begin{dependencies}
\hyperref[thm:density-of-rationals-in-reals]{Density of the Rationals}.
\end{dependencies}

\begin{theorembox}{Theorem (No Immediate Successors in $\mathbb{R}$)}
\begin{theorem}[No Immediate Successors in $\mathbb{R}$]\label{thm:no-immediate-successors-in-r}
For every $a\in\mathbb{R}$, there is no least real number greater than $a$.
\hyperref[prf:no-immediate-successors-in-r]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[cor:no-adjacent-real-numbers]{No Adjacent Real Numbers}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (No Immediate Predecessors in $\mathbb{R}$)}
\begin{theorem}[No Immediate Predecessors in $\mathbb{R}$]\label{thm:no-immediate-predecessors-in-r}
For every $a\in\mathbb{R}$, there is no greatest real number less than $a$.
\hyperref[prf:no-immediate-predecessors-in-r]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[cor:no-adjacent-real-numbers]{No Adjacent Real Numbers}
\end{itemize}
\end{dependencies}


\begin{corollarybox}{Corollary (Rational and Irrational Points in Every Open Interval)}
\begin{corollary}[Rational and Irrational Points in Every Open Interval]
\label{cor:every-open-interval-contains-rational-and-irrational}
Every open interval in $\mathbb{R}$ contains both a rational number and an irrational number.
\hyperref[prf:every-open-interval-contains-rational-and-irrational]{Go to proof.}
\end{corollary}
\end{corollarybox}

\begin{remark*}[Standard quantified statement]
\[
\forall a,b\in\mathbb{R}\;
\Bigl[
a<b\rightarrow
\bigl(\exists q\in\mathbb{Q}\;(a<q<b)\land \exists s\in\mathbb{R}\setminus\mathbb{Q}\;(a<s<b)\bigr)
\Bigr].
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
\operatorname{OrderDenseSubset}(\mathbb{Q},\mathbb{R})
\land
\operatorname{OrderDenseSubset}(\mathbb{R}\setminus\mathbb{Q},\mathbb{R}).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
\exists a,b\in\mathbb{R}\;
\Bigl[
a<b\land
\bigl(
\forall q\in\mathbb{Q}\;(q\le a\lor b\le q)
\lor
\forall s\in\mathbb{R}\setminus\mathbb{Q}\;(s\le a\lor b\le s)
\bigr)
\Bigr].
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
\neg\Bigl[
\operatorname{OrderDenseSubset}(\mathbb{Q},\mathbb{R})
\land
\operatorname{OrderDenseSubset}(\mathbb{R}\setminus\mathbb{Q},\mathbb{R})
\Bigr].
\]
\end{remark*}

\begin{remark*}[Failure modes]
The corollary fails if a nonempty real interval misses all rational points or misses all irrational points.
\end{remark*}

\begin{remark*}[Failure mode decomposition]
\[
\exists a,b\in\mathbb{R}\;(a<b)
\;\land\;
\Bigl[
\underbrace{\forall q\in\mathbb{Q}\;(q\le a\lor b\le q)}_{\text{no rational point}}
\;\lor\;
\underbrace{\forall s\in\mathbb{R}\setminus\mathbb{Q}\;(s\le a\lor b\le s)}_{\text{no irrational point}}
\Bigr].
\]
\end{remark*}

\begin{remark*}[Interpretation]
The corollary combines the two density theorems. Every real interval, no matter how small, contains representatives of both arithmetic types. Thus neither the rational numbers nor the irrational numbers occupy isolated regions of the real line.
\end{remark*}

\begin{dependencies}
\hyperref[thm:density-of-rationals-in-reals]{Theorem~\ref*{thm:density-of-rationals-in-reals}},
\hyperref[thm:density-of-irrationals-in-reals]{Theorem~\ref*{thm:density-of-irrationals-in-reals}}.
\end{dependencies}





\input{volume-iii/book-analysis-i/bounding/notes/completeness/figure-zoom-invariance}


\begin{remark*}[Zoom invariance and structural consequences]~\\
Density in $\mathbb{R}$ has a scale-invariant consequence: no matter how small a nonempty
open interval becomes, it still contains both rational and irrational points. The interleaving
of $\mathbb{Q}$ and $\mathbb{R}\setminus\mathbb{Q}$ is therefore not a large-scale phenomenon.
It persists at every interval scale.

This has three important consequences. First, the continuum does not become locally discrete
under magnification. For every nonempty open interval $(a,b)$, both
\[
\mathbb{Q}\cap(a,b)
\qquad\text{and}\qquad
(\mathbb{R}\setminus\mathbb{Q})\cap(a,b)
\]
remain nonempty, and in fact infinite. Density is therefore stable under repeated restriction
to smaller intervals.

Second, this behavior sharply distinguishes the real line from discrete numerical models.
Floating-point systems form a discrete set of representable values, so repeated magnification
eventually reaches a scale at which adjacent representable numbers leave no further point
strictly between them. At that stage, interval penetration fails in the discrete model even
though it continues to hold in $\mathbb{R}$.

Third, the Archimedean Property supplies the scale mechanism behind this behavior. Given any
positive scale $\varepsilon$, there exists $n\in\mathbb{N}$ with
\[
\frac{1}{n}<\varepsilon.
\]
This makes it possible to construct rational approximations at arbitrarily small interval
scales, which is one of the basic engines behind density arguments.

The resulting contrast is structural. Smooth mathematical models are built in a setting where
interval penetration persists at every scale, whereas finite computational models eventually
encounter a smallest representable separation. The transition from continuum behavior to
discrete behavior occurs exactly where nonempty intervals in the numerical model cease to
contain further \\representable points.
\end{remark*}

\input{volume-iii/book-analysis-i/bounding/notes/completeness/figure-density-chain}
