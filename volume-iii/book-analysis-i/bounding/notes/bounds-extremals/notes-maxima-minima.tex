\subsection{Maxima and Minima}

\begin{remark*}[Exposition]
Maxima and minima are attained sharp bounds.  They are stronger than
suprema and infima because the extremal element must belong to the
nonempty set itself.
\end{remark*}

\begin{figure}[htbp]
  \centering
  \input{volume-iii/book-analysis-i/bounding/notes/bounds-extremals/figure-maxima-minima-combinations}
  \caption{Maxima and minima are suprema and infima that are attained by the set.}
  \label{fig:maxima-minima-combinations}
\end{figure}

\begin{definitionbox}{Maximum}
\begin{definition}[Maximum]
\label{def:maximum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  An
element $m\in S$ is the \emph{maximum} of $A$ if $m\in A$ and every
element of $A$ is less than or equal to $m$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  m\in A
  \quad\text{and}\quad
  (\forall x\in A)(x\leq m).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{GreatestElement}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  A=\varnothing
  \quad\text{or}\quad
  m\notin A
  \quad\text{or}\quad
  (\exists x\in A)(m<x).
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \neg\operatorname{GreatestElement}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Maximum failure means the candidate is not an attained upper bound of
  the set.

  \item[Empty-set failure.]
  The definition is stated for nonempty subsets.
  \[
    A=\varnothing.
  \]
  \[
    \operatorname{EmptySet}(A).
  \]

  \item[Attainment failure.]
  The candidate is not an element of the set.
  \[
    m\notin A.
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \neg\operatorname{GreatestElement}(m,A,P).
  \]

  \item[Upper-bound failure.]
  Some element of the set lies strictly above the candidate.
  \[
    (\exists x\in A)(m<x).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \neg\operatorname{UpperBound}(m,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
A maximum is an upper bound that is attained by the set itself.  The
attainment condition separates maximum from supremum.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:real-upper-bound]{Upper Bound}
  \item \hyperref[def:ordered-set]{Ordered Set}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Minimum}
\begin{definition}[Minimum]
\label{def:minimum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  An
element $m\in S$ is the \emph{minimum} of $A$ if $m\in A$ and every
element of $A$ is greater than or equal to $m$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  m\in A
  \quad\text{and}\quad
  (\forall x\in A)(m\leq x).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{LeastElement}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  A=\varnothing
  \quad\text{or}\quad
  m\notin A
  \quad\text{or}\quad
  (\exists x\in A)(x<m).
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \neg\operatorname{LeastElement}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Minimum failure means the candidate is not an attained lower bound of
  the set.

  \item[Empty-set failure.]
  The definition is stated for nonempty subsets.
  \[
    A=\varnothing.
  \]
  \[
    \operatorname{EmptySet}(A).
  \]

  \item[Attainment failure.]
  The candidate is not an element of the set.
  \[
    m\notin A.
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \neg\operatorname{LeastElement}(m,A,P).
  \]

  \item[Lower-bound failure.]
  Some element of the set lies strictly below the candidate.
  \[
    (\exists x\in A)(x<m).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \neg\operatorname{LowerBound}(m,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
A minimum is a lower bound that is attained by the set itself.  The
attainment condition separates minimum from infimum.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:real-lower-bound]{Lower Bound}
  \item \hyperref[def:ordered-set]{Ordered Set}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Uniqueness of the Maximum}
\begin{proposition}[Uniqueness of the Maximum]
\label{prop:uniqueness-of-the-maximum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$m,n\in S$ are both maxima of $A$, then $m=n$.

\hyperref[prf:uniqueness-of-the-maximum]{Go to proof.}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{GreatestElement}(m,A,P)
  \quad\text{and}\quad
  \operatorname{GreatestElement}(n,A,P)
  \quad\Longrightarrow\quad
  m=n.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{GreatestElement}(m,A,P)\land\operatorname{GreatestElement}(n,A,P)
  \Longrightarrow m=n.
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{GreatestElement}(m,A,P)
  \quad\text{and}\quad
  \operatorname{GreatestElement}(n,A,P)
  \quad\text{and}\quad
  m\neq n.
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{GreatestElement}(m,A,P)\land
  \operatorname{GreatestElement}(n,A,P)\land m\neq n.
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require two distinct elements of $A$ each to dominate the
  entire set.

  \item[Distinct-maxima failure.]
  Two different candidates both satisfy the maximum predicate.
  \[
    \operatorname{GreatestElement}(m,A,P)\land
    \operatorname{GreatestElement}(n,A,P)\land m\neq n.
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{GreatestElement}(m,A,P)\land
    \operatorname{GreatestElement}(n,A,P)\land m\neq n.
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
Uniqueness says that attainment at the top of a set cannot occur at two
distinct ordered-set elements.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:maximum]{Maximum}
  \item \hyperref[def:ordered-set]{Ordered Set}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Uniqueness of the Minimum}
\begin{proposition}[Uniqueness of the Minimum]
\label{prop:uniqueness-of-the-minimum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$m,n\in S$ are both minima of $A$, then $m=n$.
\hyperref[prf:uniqueness-of-the-minimum]{\textit{Go to proof.}}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{LeastElement}(m,A,P)
  \quad\text{and}\quad
  \operatorname{LeastElement}(n,A,P)
  \quad\Longrightarrow\quad
  m=n.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{LeastElement}(m,A,P)\land\operatorname{LeastElement}(n,A,P)
  \Longrightarrow m=n.
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{LeastElement}(m,A,P)
  \quad\text{and}\quad
  \operatorname{LeastElement}(n,A,P)
  \quad\text{and}\quad
  m\neq n.
\]
\end{remark*}

\begin{remark*}[Negation predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{LeastElement}(m,A,P)\land
  \operatorname{LeastElement}(n,A,P)\land m\neq n.
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require two distinct elements of $A$ each to lie below the
  entire set.

  \item[Distinct-minima failure.]
  Two different candidates both satisfy the minimum predicate.
  \[
    \operatorname{LeastElement}(m,A,P)\land
    \operatorname{LeastElement}(n,A,P)\land m\neq n.
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{LeastElement}(m,A,P)\land
    \operatorname{LeastElement}(n,A,P)\land m\neq n.
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
Uniqueness says that attainment at the bottom of a set cannot occur at two
distinct ordered-set elements.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:minimum]{Minimum}
  \item \hyperref[def:ordered-set]{Ordered Set}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Maximum Implies Supremum}
\begin{proposition}[Maximum Implies Supremum]
\label{prop:maximum-implies-supremum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$m=\max A$, then $m=\sup A$.

\hyperref[prf:maximum-implies-supremum]{Go to proof.}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{GreatestElement}(m,A,P)
  \quad\Longrightarrow\quad
  \operatorname{LeastUpperBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{GreatestElement}(m,A,P)\Longrightarrow
  \operatorname{LeastUpperBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  \operatorname{GreatestElement}(m,A,P)
  \quad\text{and}\quad
  \neg\operatorname{LeastUpperBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require an attained upper bound that is not least among
  upper bounds.

  \item[Leastness failure.]
  The candidate is a maximum but not the least upper bound.
  \[
    \operatorname{GreatestElement}(m,A,P)\land
    \neg\operatorname{LeastUpperBound}(m,A,P).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{GreatestElement}(m,A,P)\land
    \neg\operatorname{LeastUpperBound}(m,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
An attained upper bound is automatically least among upper bounds because
any upper bound must lie above the attained element.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:maximum]{Maximum}
  \item \hyperref[def:supremum]{Supremum}
  \item \hyperref[def:real-upper-bound]{Upper Bound}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Minimum Implies Infimum}
\begin{proposition}[Minimum Implies Infimum]
\label{prop:minimum-implies-infimum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$m=\min A$, then $m=\inf A$.
\hyperref[prf:minimum-implies-infimum]{\textit{Go to proof.}}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{LeastElement}(m,A,P)
  \quad\Longrightarrow\quad
  \operatorname{GreatestLowerBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{LeastElement}(m,A,P)\Longrightarrow
  \operatorname{GreatestLowerBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  \operatorname{LeastElement}(m,A,P)
  \quad\text{and}\quad
  \neg\operatorname{GreatestLowerBound}(m,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require an attained lower bound that is not greatest among
  lower bounds.

  \item[Greatestness failure.]
  The candidate is a minimum but not the greatest lower bound.
  \[
    \operatorname{LeastElement}(m,A,P)\land
    \neg\operatorname{GreatestLowerBound}(m,A,P).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{LeastElement}(m,A,P)\land
    \neg\operatorname{GreatestLowerBound}(m,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
An attained lower bound is automatically greatest among lower bounds because
any lower bound must lie below the attained element.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:minimum]{Minimum}
  \item \hyperref[def:infimum]{Infimum}
  \item \hyperref[def:real-lower-bound]{Lower Bound}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Supremum in the Set is the Maximum}
\begin{proposition}[Supremum in the Set is the Maximum]
\label{prop:supremum-in-the-set-is-the-maximum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$s=\sup A$ and $s\in A$, then $s=\max A$.

\hyperref[prf:supremum-in-the-set-is-the-maximum]{Go to proof.}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{LeastUpperBound}(s,A,P)
  \quad\text{and}\quad
  s\in A
  \quad\Longrightarrow\quad
  \operatorname{GreatestElement}(s,A,P).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{LeastUpperBound}(s,A,P)\land s\in A
  \Longrightarrow \operatorname{GreatestElement}(s,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  \operatorname{LeastUpperBound}(s,A,P)
  \quad\text{and}\quad
  s\in A
  \quad\text{and}\quad
  \neg\operatorname{GreatestElement}(s,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require a supremum that belongs to the set but is not its
  maximum.

  \item[Attained-supremum failure.]
  The candidate is a least upper bound, belongs to the set, and yet is not
  the greatest element.
  \[
    \operatorname{LeastUpperBound}(s,A,P)\land s\in A\land
    \neg\operatorname{GreatestElement}(s,A,P).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{LeastUpperBound}(s,A,P)\land s\in A\land
    \neg\operatorname{GreatestElement}(s,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
A supremum that belongs to the set is an attained upper bound, so it is the
maximum.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:supremum]{Supremum}
  \item \hyperref[def:maximum]{Maximum}
\end{itemize}
\end{dependencies}

\begin{propositionbox}{Infimum in the Set is the Minimum}
\begin{proposition}[Infimum in the Set is the Minimum]
\label{prop:infimum-in-the-set-is-the-minimum}
Let $(S,\leq)$ be an ordered set and let $A\subseteq S$ be nonempty.  If
$i=\inf A$ and $i\in A$, then $i=\min A$.
\hyperref[prf:infimum-in-the-set-is-the-minimum]{\textit{Go to proof.}}
\end{proposition}
\end{propositionbox}

\begin{remark*}[Standard quantified statement]
\[
  A\neq\varnothing
  \quad\text{and}\quad
  \operatorname{GreatestLowerBound}(i,A,P)
  \quad\text{and}\quad
  i\in A
  \quad\Longrightarrow\quad
  \operatorname{LeastElement}(i,A,P).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  P=\mathsf{OrderedSet}(S,\leq),\qquad
  \operatorname{GreatestLowerBound}(i,A,P)\land i\in A
  \Longrightarrow \operatorname{LeastElement}(i,A,P).
\]
\end{remark*}

\begin{remark*}[Negated quantified statement]
\[
  \operatorname{GreatestLowerBound}(i,A,P)
  \quad\text{and}\quad
  i\in A
  \quad\text{and}\quad
  \neg\operatorname{LeastElement}(i,A,P).
\]
\end{remark*}

\begin{remark*}[Failure modes]
\begin{description}
  \item[Exposition.]
  Failure would require an infimum that belongs to the set but is not its
  minimum.

  \item[Attained-infimum failure.]
  The candidate is a greatest lower bound, belongs to the set, and yet is not
  the least element.
  \[
    \operatorname{GreatestLowerBound}(i,A,P)\land i\in A\land
    \neg\operatorname{LeastElement}(i,A,P).
  \]
  \[
    P=\mathsf{OrderedSet}(S,\leq),\qquad
    \operatorname{GreatestLowerBound}(i,A,P)\land i\in A\land
    \neg\operatorname{LeastElement}(i,A,P).
  \]
\end{description}
\end{remark*}

\begin{remark*}[Interpretation]
An infimum that belongs to the set is an attained lower bound, so it is the
minimum.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:infimum]{Infimum}
  \item \hyperref[def:minimum]{Minimum}
\end{itemize}
\end{dependencies}
